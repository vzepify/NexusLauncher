# Nexus Launcher Linux support

The `LinuxNexusLauncher/` directory is the native Linux implementation for the
current Nexus Launcher source. The existing Windows MFC/GDI+ project remains
unchanged.

See `LinuxNexusLauncher/README.md` for dependencies and build instructions.

Quick build:

```bash
./build-linux.sh
```
